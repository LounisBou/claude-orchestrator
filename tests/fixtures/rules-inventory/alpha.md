# Alpha

Every agent reports its measured context at the end of each report.

A session never waits on a message it has not verified reached its address.

The trace reads a pipe | inside a rule without shifting any column.
